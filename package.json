{
  "name": "fmonniot-website-tests",
  "private": true,
  "description": "Visual regression tests and screenshot tooling for the website",
  "scripts": {
    "test:visual": "playwright test visual.spec.ts",
    "test:visual:update": "playwright test visual.spec.ts --update-snapshots",
    "screenshots": "playwright test screenshots.spec.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
